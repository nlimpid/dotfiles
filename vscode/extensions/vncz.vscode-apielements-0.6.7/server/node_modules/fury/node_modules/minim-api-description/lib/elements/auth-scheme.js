'use strict';

var _get = require('babel-runtime/helpers/get')['default'];

var _inherits = require('babel-runtime/helpers/inherits')['default'];

var _createClass = require('babel-runtime/helpers/create-class')['default'];

var _classCallCheck = require('babel-runtime/helpers/class-call-check')['default'];

Object.defineProperty(exports, '__esModule', {
  value: true
});

exports['default'] = function (namespace) {
  var ArrayElement = namespace.getElementClass('array');

  var AuthScheme = (function (_ArrayElement) {
    _inherits(AuthScheme, _ArrayElement);

    function AuthScheme() {
      _classCallCheck(this, AuthScheme);

      _get(Object.getPrototypeOf(AuthScheme.prototype), 'constructor', this).apply(this, arguments);
      this.element = 'authScheme';
    }

    _createClass(AuthScheme, [{
      key: 'transitions',
      get: function get() {
        return this.children(function (item) {
          return item.element === 'transition';
        });
      }
    }, {
      key: 'members',
      get: function get() {
        return this.children(function (item) {
          return item.element === 'member';
        });
      }
    }]);

    return AuthScheme;
  })(ArrayElement);

  namespace.register('authScheme', AuthScheme);
  namespace.register('Basic Authentication Scheme', AuthScheme);
  namespace.register('Token Authentication Scheme', AuthScheme);
  namespace.register('OAuth2 Scheme', AuthScheme);
};

module.exports = exports['default'];
//# sourceMappingURL=auth-scheme.map